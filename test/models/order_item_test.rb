require 'test_helper'

class OrderItemTest < ActiveSupport::TestCase
  # test "the truth" do
  #   assert true
  # end
 puts "An order relies on 2 constraints: account_id and product_id. Since both of these cannot be created artificially, an example order cannot be created for testing purpose."
end
